name = "dn-journeys-cms"
compatibility_date = "2025-09-30"
compatibility_flags = ["nodejs_compat"]

# Main entry point (OpenNext adapter handles this)
main = ".open-next/worker.js"

[vars]
PAYLOAD_SECRET = "CHANGE-THIS-TO-A-RANDOM-32-CHAR-STRING"
SERVER_URL = "http://localhost:8787"

# D1 Database — IDs will be auto-filled after first deploy
[[d1_databases]]
binding = "DB"
database_name = "dn-journeys-db"
database_id = "YOUR_D1_DATABASE_ID"

# R2 Storage for media uploads
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "dn-journeys-media"
